[package]
name = "rpc_core"
version = "1.0.0"
edition = "2021"
description = "Delta Chat core as an Android background-service backend (JNI)"

[lib]
name = "rpc_core"
crate-type = ["cdylib"]

[dependencies]
deltachat-jsonrpc = { git = "https://github.com/chatmail/core" }
# For local development:
# deltachat-jsonrpc = { path = "../../core/deltachat-jsonrpc" }
yerpc = { version = "0.6.4", features = ["anyhow_expose"] }
tokio = { version = "1", features = ["rt-multi-thread", "sync", "net", "io-util"] }
tokio-tungstenite = "0.24"
tiny_http = "0.12"
futures-util = "0.3"
tokio-util = "0.7.18"
futures-lite = "2.6.1"
anyhow = "1"
serde_json = "1"
jni = "0.21"
once_cell = "1"
log = "0.4"
android_logger = "0.14"

[profile.release]
opt-level = "s"
lto = true
strip = true
